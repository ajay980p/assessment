# API base URL (no trailing slash). Used by the frontend to call the backend.
# For local dev, backend usually runs at http://127.0.0.1:8000
# For production, set this to your deployed API URL (e.g. https://api.yourdomain.com)
VITE_API_URL=http://127.0.0.1:8000
